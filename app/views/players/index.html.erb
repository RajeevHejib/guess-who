<br> <% if @players.any? %>
  <% @players.each do |player| %>
    <%= image_tag player.image.url(:thumb) %>
    <h6><%= player.name %></h6>
  <% end %>
<% else %>
  <h1>No players yet! Please add some by filling in the form</h1>
<% end %>

<%= link_to 'Add a player', new_player_path %>

<div class="container">
  <div class="row">
    <div class="col-sm-4"><%=image_tag @player.image.url(:thumb) %><br>
                                              <%= @player.name %> </div>
    <div class="col-sm-4">.col-md-4</div>
    <div class="col-sm-4">.col-md-4</div>
  </div>
  <div class="row">
    <div class="col-sm-4">.col-md-4</div>
    <div class="col-sm-4">.col-md-4</div>
    <div class="col-sm-4">.col-md-4</div>
  </div>
  <div class="row">
    <div class="col-sm-4">.col-md-4</div>
    <div class="col-sm-4">.col-md-4</div>
    <div class="col-sm-4">.col-md-4</div>
  </div>
</div>
